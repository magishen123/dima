[defaults]
inventory = inventory/hosts.yml
host_key_checking = False
timeout = 30
gathering = smart
forks = 10
strategy = free

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
