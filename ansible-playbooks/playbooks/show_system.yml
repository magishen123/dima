- name: show_system
  tags: show_system
  hosts: all
  connection: network_cli
  vars:
    ansible_network_os: eltex.esr.esr
    esr_commands: show system
  collections:
    - eltex.esr
  gather_facts: False
  tasks:
    - name: show system data
      include_tasks: base_tasks/commands_task.yml
